# Force legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Disable fund messages
fund=false

# Set registry
registry=https://registry.npmjs.org/

# Increase network timeout
network-timeout=300000